/// The order of the sorting algorithm.
#[derive(Clone,Copy)]
pub enum Order {
    /// The order should be sorted in ascending order.
    Ascending,
    /// The order should be sorted in descending order.
    Descending
}